// when a variable has no value, the browser automatically assigns a value of undefined
let theirAge;
console.log(theirAge, theirAge + 3, `the age is ${theirAge}`);

//eg a form, user selects their title: Mr. However they reset the form eg back to null.
let ageNull = null;
console.log(ageNull, ageNull + 3, `the ages is ${ageNull}`);
