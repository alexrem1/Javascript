// the condition is the only statement inside the parentheses

let i = 0; // initialization variable

while (i < 5) {
  console.log("in loops:", i);
  i++; // final expression: execute everytime at the end of the code block
}

const names = ["alex", "is", "ready", "always"];
let listnames = 0;

while (listnames < names.length) {
  console.log(names[listnames]);
  listnames++;
}
